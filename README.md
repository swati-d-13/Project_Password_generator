import secrets and string modules.
define a function generate_password(length).
create sets of letters, digits and symbols.
combine all characters into a single string.
Generate secrets characters based on user defined length.
shuffle the characters to make password stronger.
Return and display the generated password.
